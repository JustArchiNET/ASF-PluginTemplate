<Project>
	<Import Project="ArchiSteamFarm/Directory.Build.props" />

	<PropertyGroup Condition="$(MSBuildProjectName) != 'ArchiSteamFarm'">
		<Version>1.0.0.0</Version>
	</PropertyGroup>

	<PropertyGroup Condition="$(MSBuildProjectName) != 'ArchiSteamFarm'">
		<ApplicationIcon />
		<Authors>YourName</Authors>
		<Company>$(Authors)</Company>
		<Copyright>Copyright © 2021-$([System.DateTime]::UtcNow.Year) $(Company)</Copyright>
		<Description>MyAwesomePlugin is awesome.</Description>
		<PackageIcon />
		<PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>
		<PackageProjectUrl>https://github.com/$(Company)/MyAwesomePlugin</PackageProjectUrl>
		<PackageReleaseNotes>$(PackageProjectUrl)/releases</PackageReleaseNotes>
		<RepositoryUrl>$(PackageProjectUrl).git</RepositoryUrl>
	</PropertyGroup>

	<PropertyGroup Condition="$(MSBuildProjectName) != 'ArchiSteamFarm' AND '$(Configuration)' == 'Release'">
		<AssemblyOriginatorKeyFile>../resources/MyAwesomePlugin.snk.pub</AssemblyOriginatorKeyFile>
	</PropertyGroup>

	<!-- Public SNK signing -->
	<PropertyGroup Condition="$(MSBuildProjectName) != 'ArchiSteamFarm' AND '$(Configuration)' == 'Release' AND !EXISTS('resources/MyAwesomePlugin.snk.pub')">
		<PublicSign>false</PublicSign>
		<SignAssembly>false</SignAssembly>
	</PropertyGroup>

	<!-- Private SNK signing -->
	<PropertyGroup Condition="$(MSBuildProjectName) != 'ArchiSteamFarm' AND '$(Configuration)' == 'Release' AND EXISTS('resources/MyAwesomePlugin.snk')">
		<AssemblyOriginatorKeyFile>../resources/MyAwesomePlugin.snk</AssemblyOriginatorKeyFile>
		<PublicSign>false</PublicSign>
		<SignAssembly>true</SignAssembly>
	</PropertyGroup>
</Project>
